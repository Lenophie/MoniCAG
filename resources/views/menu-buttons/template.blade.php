<div class="@yield('width') mb-3 mb-md-3">
    <button class="btn btn-block @yield('style') menu-button">
        <span class="menu-text">@yield('title')</span>
        <br>
        @yield('icon')
    </button>
</div>